1) Line 12 would print the latest value of i into the console. (2)
2) Line 13 would print 0.5
3) Line 14 would print the latest value of finalPrice (150)
4) This function would return the entire discounted array [50, 100, 150]
5) This would raise an error as i was declared using let inside of the for loop block
6) This would raise and error as discountedPrice was declared using let inside of the for loop block
7) Line 14 would print the latest value of finalPrice (150)
8) This would print the whole discounted array [50, 100, 150]
9) This would raise an error as i was defined using let inside of the loop block
10) This would print 3
11) This would print the values of the discounted array [50, 100, 150]
12) 
    A - student.name
    B - student["Grad Year"] 
    C - student.greeting()
    D - student["Favorite Teacher"].name
    E - student.courseLoad[0]
13) 
    A - '32' number is changed into a string and then concatenated in JS
    B - 1 numeric conversion in mathematical functions happen automatically
    C - 3 null maps to 0 when converted into a number
    D - "3null" we are doing concatenation with two strings -> null is converted
    E - 4 true evaluates to 1 and we add both together
    F - 0 false and null are both converted into 0 when made into a num
    G - 3undefined we do concatenation with 2 strings
    H - NaN undefined is NaN as a number not 0
14) 
    A - True, 2 becomes a number and 2 > 1
    B - False, '2' is greater than the first character of 12 -> '1'
    C - True, after '2' gets converted into an int they are the same
    D - False, these 2 are not the same when using a strict equality that checks types
    E - False, True evaluates to 1 and 1 != 2
    F - True, any non 0 value in javascript results as True
15) The === is a strict equality. This means that it checks both value and type when comparing two operands. == is a consequence of javascript being weakly typed, meaning type conversions happen at runtime
17) the result would be the array [2, 4, 6] This is because for each number in the array, the callback function is exectuted doubling each element
19) This will output 1 4 3 2
